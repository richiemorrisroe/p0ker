[project]
name = "p0ker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "hypothesis>=6.148.7",
    "pytest>=9.0.2",
    "ty>=0.0.3",
]

[tool.pytest.ini_options]
markers = ["slow"]
